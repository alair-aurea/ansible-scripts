#
# Define the packages that should not be installed with none
#

# JDK 8
jdk8_package_name: openjdk8

# MAVEN
maven_package_name: maven

# GRADLE
gradle_package_name: gradle

# INTELLIJ
intellij_package_name: none

# YOURKIT
yourkit_package_name: none

# DOCKER
docker_package_name: none

# MySql
mysql_package_name: mysql

# Redis
redis_package_name: redis

# Chrome
chrome_package_name: chromium

# Node
node_package_name: nodejs

# npm
npm_package_name: npm
